type Props = {
    username: string,
    title: string,
    userImage: string,
    img: string,
    caption: string
}

const Post = ({username, title, userImage, img, caption}: Props) => {
	return <div>Post</div>
}

export default Post
